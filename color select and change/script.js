// Selects the first div element in the document and assigns it to the variable named box
box = document.querySelector("div");

// Changes the background color of the box variable to the provided color parameter
function bgChange(color) {
    box.style.background = color;        // Changes the background color of the box variable to the provided color parameter
}